# Copy to .env for local `vercel dev`. Production values live in Vercel dashboard.

# Supabase project: speakeasy-agent (id dquuimhmbofdhdsbdbly)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Scheduling link delivered on interview opt-in. Currently Notion Calendar.
SCHEDULING_URL=

# Kit (formerly ConvertKit) API key. If unset, Kit push is skipped.
KIT_API_KEY=
